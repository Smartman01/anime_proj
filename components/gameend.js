import Image from 'next/image'

const Gameend = ({winlose, name, image, animes}) => {
    return (
        <div style={{
            display: 'flex', flexDirection: 'column', alignItems: 'center',
            justifyContent: 'center'
        }}>
            {/* <Image /> */}
        </div>
    );
}

export default Gameend;